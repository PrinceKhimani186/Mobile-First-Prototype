I have added the following Replit secrets:

* SUPABASE_URL
* SUPABASE_ANON_KEY

Now complete the Supabase integration.

## Step 1 - Create Database Tables

Generate the SQL migration needed for Supabase.

Do NOT ask me to manually design the tables.

Create a table named:

customer_enrollment

Columns:

* id (uuid, primary key)
* created_at
* updated_at
* full_name
* email (unique)
* phone
* company_name
* country
* business_type
* preferred_contact
* selected_package
* document_name
* document_url
* onboarding_status
* payment_status
* password_created
* game_selected
* customization_completed
* dashboard_completed

Generate the SQL file and tell me exactly where to run it in the Supabase SQL Editor.

---

## Step 2 - Create Storage Bucket

Generate the SQL or instructions to create a Storage bucket called:

customer-documents

This bucket will store uploaded enrollment documents.

Do not store files inside PostgreSQL.

---

## Step 3 - Connect Enrollment Form

When the customer submits the Enrollment Form:

Validate:

* Name
* Email
* Phone
* Company
* Country
* Business Type
* Required Document

If validation succeeds:

* Upload the document to Supabase Storage.
* Save the customer information in the customer_enrollment table.

Set default values:

payment_status = 'pending'

password_created = false

game_selected = false

customization_completed = false

dashboard_completed = false

selected_package = null

onboarding_status = 'enrollment_completed'

---

## Step 4 - Package Selection

After enrollment is saved successfully:

Redirect to Package Selection.

When the user selects:

* Essentials
* Accelerator
* Empire

Update the existing customer row.

Do NOT insert another row.

Update:

selected_package

---

## Step 5 - Stripe Success

After Stripe payment succeeds:

Update the same customer.

Set:

payment_status = 'paid'

---

## Step 6 - Password Setup

After the user creates their password:

Update:

password_created = true

---

## Step 7 - Login

After login:

Read onboarding progress from Supabase.

Redirect using these rules:

If game_selected = false

→ /onboarding/game-selection

Else if customization_completed = false

→ /onboarding/customization

Else

→ /onboarding/dashboard

---

## Step 8 - Route Protection

Prevent users from opening any onboarding page out of order.

Automatically redirect them to the first incomplete step.

---

## Step 9 - Error Handling

If database insert fails:

Show:

"Unable to save your information."

If upload fails:

Show:

"Unable to upload document."

Do not continue until both operations succeed.

---

## Step 10 - Keep UI Unchanged

Do NOT modify:

* Design
* Styling
* Layout
* Colors
* Animations

Only implement the backend integration and onboarding flow.

---

## Step 11 - Service Role Key

For now, use only the existing SUPABASE_URL and SUPABASE_ANON_KEY.

If a backend operation later requires the Service Role Key, clearly identify the exact functionality that depends on it and tell me where to add the key in Replit Secrets. Do not block the rest of the integration waiting for it.
